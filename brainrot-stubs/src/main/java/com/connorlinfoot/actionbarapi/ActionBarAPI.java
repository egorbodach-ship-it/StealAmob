package com.connorlinfoot.actionbarapi;

import org.bukkit.entity.Player;

/**
 * Compile-time stub for ActionBarAPI. Not bundled into plugin jars (provided scope);
 * the real ActionBarAPI plugin supplies these classes at runtime.
 */
public class ActionBarAPI {
    public static void sendActionBar(Player player, String message) {}
    public static void sendActionBar(Player player, String message, int duration) {}
}
